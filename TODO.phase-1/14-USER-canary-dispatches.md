= USER: canary fetch dispatches (ch/uk -> ca/un/eu -> us)
Each canary: workflow_dispatch + verify tracked output commits + only then re-enable cron.
Remote actions — per-action approval class. Blocked by: 01,02,03.

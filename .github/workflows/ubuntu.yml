# Auto-generated by Cimas: Do not edit it manually!
# See https://github.com/metanorma/cimas
name: ubuntu

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ '**' ]

jobs:
  test-linux:
    name: Test on Ruby ${{ matrix.ruby }} Ubuntu
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        ruby: [ '2.6' ]
    steps:
      - uses: actions/checkout@master
      - name: Use Ruby
        uses: actions/setup-ruby@v1
        with:
          ruby-version: ${{ matrix.ruby }}
          architecture: 'x64'
      - name: Update gems
        run: |
          gem install bundler
          bundle install --jobs 4 --retry 3
      - name: Preparing data sources
        uses: actions/checkout@master
        with:
          repository: ammitto/eu-data
          path: '~/.ammitto/sources/eu-data'
      - name: Show Update Log
        shell: bash
        run: |
         cd ~/.ammitto/sources/ && ls